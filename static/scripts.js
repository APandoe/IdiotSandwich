// $( document ).ready(function() { //wait until body loads

// 		var meal_chosen=false; //keeps track of whether the testimonial box is filled out

// 		//Inputs that determine what fields to show
// 		var meal = $('#live_form input:radio[name=meal]');

// 		meal.change(function(){ //when the meal changes
// 			var value=this.value;
// 			all.addClass('hidden'); //hide everything and reveal as needed

// 			if (meal_chosen==true){
// 				name.removeClass('hidden'); //show recipes
// 			}
// 		});
// });

function showRecipes(ingredients)
{
		var input

};

function test(event)
{
	console.log("blah");
};